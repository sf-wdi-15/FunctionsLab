// Create a function called numSquare that will return an array of all perfect 
// square numbers up to, but not exceeding a max number.

// helped by Mark

var maxVal = 10
var = arraySquares []

numSquare = function(max){

  for (var i = 0; i <=maxVal; i++){
    square = Math.pow(i, 2);
    arraySquares.push(square);
  }
};